class Solution {
public:
    int majorityElement(vector<int>& nums) {
        int i,j;
        int l=nums.size();
        sort(nums.begin(), nums.end(), greater<int>());
        return nums[(l-1)/2];
    }
};
